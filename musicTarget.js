export default class MusicTarget {
    constructor(name, body, sound, removed = false) {
        this.name = name;
        this.body = body;
        this.sound = sound;
        this.removed = removed
    }
}

